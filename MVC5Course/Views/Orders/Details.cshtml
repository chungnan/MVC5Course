@model MVC5Course.Models.Order

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Order</h4>
    <hr />

    <h3>使用 Html.DisplayFor(m => m)</h3>
    @Html.DisplayFor(m => m)

    <h3>使用 Html.DisplayForModel</h3>
    @Html.DisplayForModel()
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.OrderId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
